package edu.ufp.inf.projeto.JavaFXStuff;
/*
import javafx.scene.control.*;
import javafx.scene.layout.VBox;

import java.time.LocalDate;

public class PesquisaPane extends VBox {

    private ComboBox<String> tipoPesquisaComboBox;
    private TextField parametroField;
    private DatePicker inicioDatePicker;
    private DatePicker fimDatePicker;
    private Button pesquisarButton;

    public PesquisaPane() {
        tipoPesquisaComboBox = new ComboBox<>();
        tipoPesquisaComboBox.getItems().addAll("Por Jornal", "Por Conferência", "Por Autor");

        parametroField = new TextField();
        parametroField.setPromptText("Parâmetro de Pesquisa");

        inicioDatePicker = new DatePicker();
        fimDatePicker = new DatePicker();

        pesquisarButton = new Button("Pesquisar");

        getChildren().addAll(new Label("Tipo de Pesquisa"), tipoPesquisaComboBox, new Label("Parâmetro"), parametroField,
                new Label("Data de Início"), inicioDatePicker, new Label("Data de Fim"), fimDatePicker, pesquisarButton);
    }

    public String getTipoPesquisa() {
        return tipoPesquisaComboBox.getValue();
    }

    public String getParametro() {
        return parametroField.getText();
    }

    public LocalDate getDataInicio() {
        return inicioDatePicker.getValue();
    }

    public LocalDate getDataFim() {
        return fimDatePicker.getValue();
    }

    public Button getPesquisarButton() {
        return pesquisarButton;
    }
}
*/
